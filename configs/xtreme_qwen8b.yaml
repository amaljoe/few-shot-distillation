# Qwen3-8B configuration for XTREME multi-task experiments

model:
  name: "Qwen/Qwen3-8B"
  num_layers: 36
  hidden_size: 4096
  thinking_mode: false

data:
  tasks: [nli, pa, qa, ner, pos]
  train_lang: en
  eval_langs: [en, hi, es, de, fr, zh]
  max_samples_per_task: 5000
  max_seq_len_teacher: 2048
  max_seq_len_student: 512
  num_workers: 4

training:
  seed: 42
  output_dir: "experiments/xtreme/qwen8b"
  logging_steps: 10
  save_steps: 200
  max_steps: 1000
  warmup_steps: 50
  lr: 2.0e-4
  weight_decay: 0.01
  grad_clip: 1.0
  per_device_train_batch_size: 2
  gradient_accumulation_steps: 8
  bf16: true
  gradient_checkpointing: true
  use_lora: true

lora:
  r: 16
  alpha: 32
  dropout: 0.05
  target_modules:
    - q_proj
    - k_proj
    - v_proj
    - o_proj
    - gate_proj
    - up_proj
    - down_proj
  bias: "none"
